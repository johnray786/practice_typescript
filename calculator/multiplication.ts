export function multiplication (num1: number, num2: number, op:string) {
    console.log(`${num1} ${op} ${num2} = `);
    const multiresult = num1*num2;
    console.log(multiresult);
    return multiresult;
}
