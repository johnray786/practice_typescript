export function substraction (num1: number, num2: number, op:string) {
    console.log(`${num1} ${op} ${num2} = `);
    const subsresult = num1-num2;
    console.log(subsresult);
    return subsresult;
}
